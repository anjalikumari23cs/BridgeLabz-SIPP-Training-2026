SELECT country,
       vaccinated_count AS population_count,
       'Vaccinated' AS category
FROM vaccinated_population

UNION

SELECT country,
       unvaccinated_count AS population_count,
       'Unvaccinated' AS category
FROM unvaccinated_population;